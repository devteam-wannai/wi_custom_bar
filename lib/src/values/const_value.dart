class ConstValue {
  // base vertical width of index bar
  static const double baseVerticalWidth = 30;
  // base vertical height of index bar
  static const double baseVerticalHeight = 120;

  // base horizontal width of index bar
  static const double baseHorizontalWidth = 120;
  // base horizontal height of index bar
  static const double baseHorizontalHeight = 30;
}